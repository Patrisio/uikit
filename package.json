{
    "name": "@my-org/ui-kit",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "lerna run build",
        "lint": "lerna run lint",
        "release": "semantic-release"
    },
    "devDependencies": {
        "@types/react": "^19.2.10",
        "lerna": "^7",
        "semantic-release": "^22",
        "typescript": "^5"
    }
}
